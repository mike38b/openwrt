--- a/hif/fwcmd.c
+++ b/hif/fwcmd.c
@@ -1147,6 +1147,9 @@ int mwl_fwcmd_max_tx_power(struct ieee80
 	int i, tmp;
 	int rc = 0;
 
+	// do not set tx power
+	return rc;
+
 	if ((priv->chip_type != MWL8997) && (priv->forbidden_setting))
 		return rc;
 
@@ -1194,6 +1197,13 @@ int mwl_fwcmd_max_tx_power(struct ieee80
 		else
 			sub_ch = EXT_CH_BELOW_CTRL_CH;
 		break;
+	case NL80211_CHAN_WIDTH_160:
+		width = CH_160_MHZ_WIDTH;
+		if (conf->chandef.center_freq1 > channel->center_freq)
+			sub_ch = EXT_CH_ABOVE_CTRL_CH;
+		else
+			sub_ch = EXT_CH_BELOW_CTRL_CH;
+		break;
 	default:
 		return -EINVAL;
 	}
@@ -1256,6 +1266,9 @@ int mwl_fwcmd_tx_power(struct ieee80211_
 	int i, tmp;
 	int rc = 0;
 
+	// do not set tx power
+	return rc;
+
 	if ((priv->chip_type != MWL8997) && (priv->forbidden_setting))
 		return rc;
 
@@ -1303,6 +1316,13 @@ int mwl_fwcmd_tx_power(struct ieee80211_
 		else
 			sub_ch = EXT_CH_BELOW_CTRL_CH;
 		break;
+	case NL80211_CHAN_WIDTH_160:
+		width = CH_160_MHZ_WIDTH;
+		if (conf->chandef.center_freq1 > channel->center_freq)
+			sub_ch = EXT_CH_ABOVE_CTRL_CH;
+		else
+			sub_ch = EXT_CH_BELOW_CTRL_CH;
+		break;
 	default:
 		return -EINVAL;
 	}
@@ -3194,6 +3214,10 @@ int mwl_fwcmd_set_cdd(struct ieee80211_h
 	struct mwl_priv *priv = hw->priv;
 	struct hostcmd_cmd_set_cdd *pcmd;
 
+	// force-enable Cyclic delay diversity
+	// https://en.wikipedia.org/wiki/Cyclic_delay_diversity
+	priv->cdd = 1;
+
 	pcmd = (struct hostcmd_cmd_set_cdd *)&priv->pcmd_buf[0];
 
 	mutex_lock(&priv->fwcmd_mutex);
@@ -3469,6 +3493,9 @@ int mwl_fwcmd_get_fw_region_code_sc4(str
 	struct hostcmd_cmd_get_fw_region_code_sc4 *pcmd;
 	u16 cmd;
 
+	// do not load the firmware region
+	return -EIO;
+
 	pcmd = (struct hostcmd_cmd_get_fw_region_code_sc4 *)&priv->pcmd_buf[0];
 
 	mutex_lock(&priv->fwcmd_mutex);
@@ -3509,6 +3536,9 @@ int mwl_fwcmd_get_pwr_tbl_sc4(struct iee
 	int status;
 	u16 cmd;
 
+	// do not load the regulatory power table
+	return -EIO;
+
 	pcmd = (struct hostcmd_cmd_get_device_pwr_tbl_sc4 *)&priv->pcmd_buf[0];
 
 	mutex_lock(&priv->fwcmd_mutex);
--- a/core.c
+++ b/core.c
@@ -139,7 +139,7 @@ struct region_code_mapping {
 static const struct region_code_mapping regmap[] = {
 	{"US", 0x10}, /* US FCC */
 	{"CA", 0x20}, /* Canada */
-	{"FR", 0x30}, /* France */
+	{"BG", 0x30}, /* Bulgaria */
 	{"ES", 0x31}, /* Spain  */
 	{"FR", 0x32}, /* France */
 	{"JP", 0x40}, /* Japan  */
